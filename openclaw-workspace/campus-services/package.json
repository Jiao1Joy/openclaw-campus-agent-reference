{
  "name": "campus-services",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic campus business services (SQLite, approval rules, leave state machine) for the OpenClaw campus demo.",
  "type": "module",
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "test": "node --test src/test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0"
  }
}
