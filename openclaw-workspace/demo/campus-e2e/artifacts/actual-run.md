# 校园助手端到端演示 · 实际运行结果

- 运行时间：2026-08-21T15:26:06+08:00
- 总体结果：通过
- 隔离方式：系统临时目录；仅复制 course-data.json，所有业务状态与证据均写入副本
- 现有 demo 数据未改动：true

## 请假影响、确认、幂等与审计

- 待确认预览：`awaiting-confirmation`；命中 1 门课（计算机网络）。
- 预览阶段写入请假：`false`。
- 确认后首次提交：`evaluating`；首次调用幂等命中：`false`。
- 同键重放：`true`；返回同一申请：`true`。
- 审计校验：`ok=true`，3 个事件，问题数 0。

## 选课提交、故障恢复与回滚

- 待确认方案：新增 10.0 学分；课程为 操作系统、大学体育（四）· 篮球、开源软件工程实践、现代书法鉴赏。
- 确认提交：`submitted`；提交前复核：`true`。
- 故障注入：临时副本中 `CS301-01` 名额从 46 写到 47，同时留下未完成事务日志。
- 自动恢复：`recovered=true`；名额恢复：`true`；事务日志已清除：`true`。
- 运营补偿回滚：`rolled-back`；同键重放：`true`。
- 回滚后临时课程数据恢复基线：`true`。
- 审计校验：`ok=true`，7 个事件，问题数 0。

## 源数据保护

- 运行前文件数：5；运行后文件数：5。
- 所有源文件 SHA-256 映射一致：`true`。
