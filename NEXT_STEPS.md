# OpenClaw 智能校园助手：后续功能拓展路线

> 更新日期：2026-08-12
> 当前定位：OpenClaw 场景 Agent 演示与能力验证项目

## 1. 产品定位

本项目的核心不是建设一套完整的学校门户或教务系统，而是拓展和验证 OpenClaw 在智能校园场景中的 Agent 能力。

- **主产品**：OpenClaw 智能校园助手，包括技能发现、意图路由、工具调用、确认执行、状态恢复、运行追踪和安全治理。
- **演示外壳**：校园门户页面，用于展示用户如何进入和使用 OpenClaw 助手。
- **演示技能**：请假、选课、校园知识问答，用于验证不同类型的工具流程。
- **暂缓内容**：真实学校数据、真实教务接口、正式审批流、学校统一登录、生产数据库和校级部署。

“云川大学”是内置 Demo 租户，页面中的学生、课程、教师、制度和办理结果均为演示数据，不代表真实学校。

## 2. 已完成的 OpenClaw 基础能力

- 独立 `campus` Agent、隔离工作区和受限工具权限；
- 本地 Skill 发现与调用，请假、选课、知识问答三种工具模式可完整演示；
- 服务端注入受信上下文，隔离不同用户的会话；
- 写操作必须先展示摘要并获得明确确认；
- 请求超时、幂等键、并发合并、角色权限和统一错误响应；
- API、请假、选课的可校验审计证据；
- 请假撤销、选课补偿回滚和异常事务恢复；
- OpenClaw 调用失败时不伪造成功结果；
- 独立前后端构建、API 集成测试和业务引擎隔离测试；
- OIDC/JWKS 身份适配能力已预留，但不继续投入真实学校接入。

这些安全能力保留为 OpenClaw 工具执行层的通用底座，而不是继续扩建学校业务系统。

## 3. 下一阶段主线：OpenClaw 能力拓展

### P0：能力注册与自动路由

当前进展：**LLM 路由第一版已完成**。四项 Demo Skill 已注册统一能力元数据，提供按角色过滤的 `/api/campus-assistant/capabilities` 接口；门户能力入口动态读取。每轮请求先经过独立 OpenClaw `campus-router` Agent，由 Qwen 27B 根据能力清单和执行上下文进行意图理解与结构化参数提取。服务端校验能力白名单、置信度和参数协议后才允许进入工具链，生产主流程不再由正则模式决定能力。

建立统一的能力清单，让前端和服务端不再硬编码“请假、选课、知识问答”。每个 Skill 对外声明：

- 能力 ID、名称、说明和版本；
- 可处理的意图与示例；
- 是否只读、是否产生外部写入；
- 所需身份、角色和确认级别；
- 输入、输出和错误结构；
- 超时、重试与幂等策略；
- 可使用的结果卡片类型。

验收目标：增加一个新 Demo Skill 时，只需注册能力和实现工具，不必修改聊天主流程。

### P0：通用执行状态机

当前进展：**第一版已完成**。通用状态已覆盖 `collecting`、`awaiting-input`、`awaiting-confirmation`、`executing`、`succeeded`、`cancelled`、`failed` 和 `expired`；状态通过原子文件持久化，选课的教师选择与确认令牌可在服务重启后恢复，门户会显示当前执行摘要。内部上下文不会通过 API 暴露。下一步是把状态迁移完全收敛到统一 Skill 执行适配器，减少请假 Skill 对自然语言回复特征的依赖。

把目前散落在请假和选课中的确认逻辑抽象成 OpenClaw 通用执行流程：

```text
理解请求 -> 选择技能 -> 收集参数 -> 生成执行预览
         -> 等待确认 -> 执行工具 -> 返回证据
                     -> 取消 / 超时 / 失败 / 回滚
```

统一保存：会话、当前阶段、待确认动作、参数摘要、过期时间、执行结果和回滚引用。服务重启后仍可恢复未完成的演示任务。

验收目标：任何有副作用的新 Skill 都自动获得确认、幂等、审计和回滚挂钩，不再重复编写一套业务控制逻辑。

### P0：运行追踪与可观测性

当前进展：**第一版已完成**。每轮请求会生成持久化 JSONL Trace，记录能力路由、OpenClaw/规则工具开始与完成、分层耗时、状态结果、幂等复用、取消和错误；支持按请求或执行编号查询，并按身份哈希隔离。门户已增加可展开的“本次助手如何完成”时间线。Trace 使用字段白名单，不保存对话正文、完整身份、访问令牌、业务参数或内部方案令牌。

为每轮 OpenClaw 调用生成脱敏 Trace：

- 请求 ID、会话 ID 和能力 ID；
- 意图路由结果；
- 使用的 Skill 与工具；
- 模型、工具和总耗时；
- 超时、重试、取消和失败阶段；
- 确认记录、执行证据和回滚关联；
- 不保存完整学号、令牌或敏感对话内容。

门户增加一个“本次助手如何完成”演示面板，让观众能看懂 OpenClaw 选择了什么能力、调用了什么工具、是否经过确认，以及结果是否真正落盘。

### P1：技能开发约定与演示 Skill 扩展

当前进展：**开发标准第一版与首个多 Skill 编排已完成**。能力注册表 2.0 会自动扫描每个 Skill 的 `capability.json`；已定义 `campus-skill-input@1` / `campus-skill-output@1` JSON-stdio 信封、运行前授权与确认拦截、输出/卡片校验、标准模板和构建期校验。原有三项 Skill 已通过 manifest 注册；新增的 `campus-leave-impact` 使用正式 JSON-stdio 运行时，顺序组合课程查询和请假预览。

提供可复制的 Skill 模板、结构校验器和测试夹具。新增技能统一使用 JSON 输入输出信封，避免依赖自然语言解析脚本输出。

建议新增的演示能力优先体现 OpenClaw 能力差异，而不是堆校园数据：

- **校园任务规划**：把“新生报到要做什么”拆成可勾选任务，只使用演示步骤；
- **多技能编排**：~~例如“明天请假并告诉我会错过哪些课”，串联课程查询和请假预览；~~ 第一版已完成
- **失败恢复演示**：模拟工具超时、重试和人工确认回滚；
- **知识转行动**：从演示知识答案生成待确认任务，但不假装已向真实部门提交。

### P1：通用结果卡片

当前进展：**第一版已完成**。聊天响应统一返回白名单 `cards[]`，首批覆盖教师选择、知识来源和执行结果；前端使用判别类型统一渲染。选课和知识卡片直接来自确定性服务端数据，不再解析模型输出的机器标记。协议限制动作类型、HTTPS 来源、字段长度、数组和卡片数量，并有恶意 URL/动作测试。

将教师卡片、知识来源卡片和操作结果提示抽象为由服务端返回的安全 UI 描述。前端只渲染白名单组件：

- 信息来源卡；
- 方案预览卡；
- 明确确认卡；
- 工具执行结果卡；
- 运行追踪卡；
- 失败、重试与回滚卡。

验收目标：新 Skill 可以复用卡片协议，无需在聊天组件中增加大量专用判断。

### P1：回归评测与安全演练

建立 OpenClaw 场景评测集，重点评估：

- 是否选对 Skill；
- 是否在缺少信息时正确补充参数；
- 是否阻止未确认写入；
- 是否抵抗消息中的身份替换和工具越权指令；
- 是否在工具超时、无效 JSON 和进程重启后给出真实状态；
- 是否只依据可信检索结果回答事实；
- 是否能复现并验证执行证据。

评测使用固定 Demo 数据，不追求覆盖真实学校规则。

## 4. 学校 Demo 的投入边界

现有三组校园内容只做“足以演示”：

- 请假：能收集参数、预览、确认、提交、查询和取消；
- 选课：能分析、选择、预览、确认、提交和回滚；
- 知识问答：能检索、展示来源、拒绝无依据回答；
- 门户：首页能打开助手并展示关键交互即可。

以下内容本阶段不再继续：

- 扩充大量课程、教师、学生或校规数据；
- 复刻真实教务、审批、排课、学籍或通知系统；
- 完成某所学校的正式 SSO 对接；
- 为 Demo 表单补齐真实附件、支付、短信或消息推送；
- 以生产上线为目标进行校级数据库和基础设施建设。

只有在 OpenClaw 通用能力需要验证时，才增加最小量的校园样例。

## 5. 推荐实施顺序

1. ~~新增能力注册表和 `/capabilities` 查询接口；~~ 已完成第一版
2. ~~建立通用执行状态与待确认动作模型；~~ 已完成第一版
3. ~~为聊天请求补充脱敏运行 Trace，并在页面提供可视化入口；~~ 已完成第一版
4. ~~把现有三项技能接入统一能力协议；~~ 已完成第一版
5. ~~增加一个多技能编排 Demo；~~ 已完成第一版
6. 补充路由、确认、越权、超时、恢复和回滚评测。

下一阶段进入第 6 项“回归评测与安全演练”，优先验证 OpenClaw 能力选择、参数补充、跨 Skill 确认、异常恢复和证据一致性，不扩充学校业务内容。
