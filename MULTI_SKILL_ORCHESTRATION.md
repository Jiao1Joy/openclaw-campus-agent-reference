# OpenClaw 多 Skill 编排 Demo

> 状态：第一版完成
> 更新日期：2026-08-12

## 目标与边界

本功能用于验证 OpenClaw 能否把多个独立能力组合成一个安全、可恢复、可观察的任务。校园课程和请假只是最小 Demo 数据，不代表真实教务或审批系统。

示例请求：

```text
我在 2026-08-17 14:00-16:00 请病假会错过哪些课？因为需要去医院检查
```

## 能力链路

```text
OpenClaw campus-router（LLM 理解意图、日期、时间、类型和原因）
  -> campus.leave-impact
  -> campus.course：只读查询当天可能受影响的已选 Demo 课程
  -> campus.leave：只读生成请假预览并检查必需参数
  -> campus-leave-impact：组合结果卡片
  -> awaiting-confirmation：等待用户明确确认
  -> campus.leave：创建独立写执行并提交 Demo 申请
```

生产主路由不再依赖 `routing.patterns` 正则命中。Manifest 中的旧模式仅保留用于兼容测试和迁移检查；真实请求由 OpenClaw LLM 根据动态能力清单选择能力。模型输出必须通过白名单、置信度、枚举、日期与时间格式校验后才会执行。

编排 Skill 的清单通过 `orchestration.dependencies` 声明两个依赖，注册表启动时检查依赖是否存在且已启用。门户通过 `orchestration-summary` 卡片展示子步骤、课程影响、缺失参数和 Demo 标识。

## 安全规则

- 课程影响查询和请假预览均为只读，不创建申请；
- 写入前必须具备日期、精确起止时间、请假类型和原因；
- “上午”“下午”等模糊时段只用于筛选课程，不能作为提交时间；
- 只有独立的明确确认消息才会触发写入；
- 写入由 `campus.leave` 独立执行记录承接，不把编排能力伪装成写能力；
- 确认请求使用幂等键，重复请求不会创建第二条申请；
- 每个子工具产生脱敏 Trace，失败状态会持久化；
- 提交结果必须返回真实申请编号，否则按失败处理；
- 所有课程、学生和办理结果均为本地 Demo。

## 当前验收结果

- 能力自动发现与依赖校验通过；
- OpenClaw LLM 可识别非模板表达的组合意图并返回结构化参数；
- 编排输出符合 `campus-skill-output@1`；
- 缺少精确时间时保持收集状态；
- 完整预览进入等待确认且不写入；
- 明确确认后产生隔离的 Demo 请假记录与申请编号；
- 重复确认请求幂等重放，不产生第二条记录；
- Trace 按顺序记录 `campus-course`、`campus-leave`、`campus-leave-impact`；
- 编排和提交失败均进入终态并清理私有上下文。

## 后续方向

下一步不增加更多学校资料，而是建立 OpenClaw 回归评测集，覆盖路由、参数追问、越权、确认绕过、超时、进程异常、重启恢复、幂等冲突和审计证据一致性。
